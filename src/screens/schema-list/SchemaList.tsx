import React from 'react';
import {
    Box,
} from '@mui/material';

export default function SchemaList() {
    return (
        <Box sx={{ display: 'flex' }}>
            Schema list
        </Box>
    )
}
